# `mix`

> Stub — fill in when the primitive ships.
>
> Planned shape: `mix([sig_a, sig_b, ...]) -> Signal`. Sums an array
> of signals. Output is the per-sample sum; the caller is responsible
> for avoiding clipping.
