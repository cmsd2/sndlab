# `patch`

> Stub — fill in when the primitive ships in [task 7](../introduction.md).
>
> Planned shape:
>
> ```rhai
> patch("name", "one_shot" | "ambient", || { /* signal */ })
> ```
>
> Sections to write: signature, semantics, name collisions, role
> semantics cross-link to [Roles](../concepts/roles.md), error cases
> (re-registration, evaluation failure), and a small example.
