# `gain`

> Stub — fill in when the primitive ships.
>
> Planned shape: `signal.gain(linear) -> Signal`. Multiplies all samples
> by the given factor. Decibel-aware variants (`gain_db`) likely added
> later.
