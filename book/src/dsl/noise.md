# `noise`

> Stub — fill in when the primitive ships.
>
> Planned shape: `noise(kind, duration_s) -> Signal` where `kind` is
> one of `"white"`, `"pink"`, `"brown"`. Deterministic per-patch RNG
> seed so re-evaluating produces the same buffer.
