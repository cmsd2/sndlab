# `env`

> Stub — fill in when the primitive ships.
>
> Planned shape: `signal.env(attack_s, decay_s) -> Signal`. Applies an
> attack ramp and an exponential decay to the signal. `decay_s` is the
> time constant — larger values mean a longer ring.
