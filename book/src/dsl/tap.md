# `tap`

> Stub — fill in when the primitive ships.
>
> Planned shape: `tap(delay_s, gain) -> Tap`, used as
> `signal.with_taps([tap(...), tap(...)])`. Each tap copies the source
> at a delayed offset with a multiplicative gain, building up a
> discrete reverb tail without a full convolution.
