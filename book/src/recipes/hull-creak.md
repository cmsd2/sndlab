# Hull creak

> Stub. The hull creak is a sparse band-passed noise burst with an
> envelope that ramps slowly up and decays. The recipe writes itself
> once the DSL has noise + envelope + band-pass filtering.
