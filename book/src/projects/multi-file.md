# Multi-file projects

> Pending the project-model work (task 9). When complete, this chapter
> covers:
>
> - The single-namespace model (all loaded scripts share the patch
>   namespace).
> - Switching between open files in the editor.
> - Conventions for splitting (e.g. `ambience.rhai`, `weapons.rhai`,
>   `ui.rhai`).
