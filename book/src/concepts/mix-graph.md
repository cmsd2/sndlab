# The mix graph

> Pending the mix-model work (task 10). When complete, this chapter
> describes:
>
> - The bus topology: master, ambient bus, one-shot bus.
> - Scene-arm semantics: which patches are armed and how a scene-fire
>   keypress triggers them simultaneously.
> - Per-patch gain (from the manifest) and the master-fader behaviour.
> - The Kira `Track` hierarchy that backs all of this.
